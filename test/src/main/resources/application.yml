application:
  name: REGISTRATION-SERVICE

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/online_banking_db
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8082
